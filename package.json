{
  "name": "server",
  "version": "1.0.0",
  "description": "Client side server for twitterapp",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js",
    "dev": "concurrently \"npm run start\" \"npm run watch --prefix client\" "
  },
  "author": "Nathan McCraw",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.4",
    "concurrently": "^6.2.1",
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
